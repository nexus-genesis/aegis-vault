{
  "name": "aegis-agent-mcp",
  "version": "0.3.0",
  "description": "MCP Server for Aegis Vault — agent key custody & delegated authorization over MCP. Generate self-sovereign PQC identities, enforce tiered spend limits / takeover / session narrowing, coordinate tasks and governance via PQC-signed writes. Private keys never leave the caller.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "aegis-agent-mcp": "src/index.js",
    "aegis-keeper": "src/keeper-main.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai-agents",
    "agent-security",
    "key-management",
    "post-quantum",
    "self-custody",
    "human-takeover",
    "dilithium2",
    "claude",
    "cursor"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "ethers": "^6.16.0",
    "aegis-agent-sdk": "^0.3.0",
    "aegis-chain-adapters": "^0.2.2",
    "aegis-chain-eth": "^0.3.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nexus-genesis/aegis-vault.git",
    "directory": "mcp-server"
  }
}
